<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>


    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="/">Home</a>
            </li>

        {#  -->pour reserver à l'admin
        {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item active">N° HYPER SECRET
            </li>
        {% endif %} #}

        {% if app.user %}
            <li class="nav-item">
                <a class="nav-link" href="{{path('category')}}">Liste catégories</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('ajoutProduit')}}">Ajout Produit</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('ajoutCategory')}}">Ajout Categorie</a>
            </li>
        {% endif %}


            <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-success" type="submit">Search</button>
        </ul>

            {% if app.user %}
            <div class="d-flex">
                <a href="{{ path('app_logout') }}">Se déconnecter de {{ app.user.email }}</a>
                <p class="btn btn-outline-success"> </p>
            </div>
            {% endif %}

            {% if not app.user %}
            <div class="d-flex">
            <a href="{{ path('app_login') }}"></a><p class="btn btn-outline-success" > Connection</p>
            </div>
            {% endif %}
        
    </div>
</nav>