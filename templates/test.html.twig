{% extends 'base.html.twig' %}

    {% block body %}

    <div>
        <div class="container">

            <div class = "row" style="border:1px solid black" >
            <div class="col" style="border:1px solid red">
                {#['count'=>dd($count)]#}
                {% for item in products %}
                {{item.name}} / {{item.price}} / {{item.slug}} <br>
                <br>
                <br>
                {% endfor %}

            </div>

            <div class="col" style="border:1px solid red">

            </div>
        </div>
 

    {% endblock %}
